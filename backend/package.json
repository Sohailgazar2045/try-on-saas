{
  "name": "virtual-tryon-backend",









































































export const specs = swaggerJsdoc(options);};  apis: ['./src/routes/*.js']  },    }      }        }          }            error: { type: 'string' }            message: { type: 'string' },          properties: {          type: 'object',        Error: {        },          }            createdAt: { type: 'string', format: 'date-time' }            type: { type: 'string', enum: ['user', 'outfit', 'generated'] },            url: { type: 'string', format: 'uri' },            id: { type: 'string' },          properties: {          type: 'object',        Image: {        },          }            createdAt: { type: 'string', format: 'date-time' }            subscription: { type: 'string', enum: ['free', 'basic', 'premium'] },            credits: { type: 'integer', minimum: 0 },            name: { type: 'string' },            email: { type: 'string', format: 'email' },            id: { type: 'string', description: 'User ID' },          properties: {          type: 'object',        User: {      schemas: {      },        }          description: 'Cookie-based JWT token'          name: 'token',          in: 'cookie',          type: 'apiKey',        cookieAuth: {        },          description: 'JWT Bearer token'          bearerFormat: 'JWT',          scheme: 'bearer',          type: 'http',        bearerAuth: {      securitySchemes: {    components: {    ],      }        description: 'Production server'        url: 'https://api.virtualtry-on.com',      {      },        description: 'Development server'        url: 'http://localhost:5000',      {    servers: [    },      }        url: 'https://virtualtry-on.com'        name: 'Support',      contact: {      description: 'AI-powered virtual try-on SaaS backend API',      version: '1.0.0',      title: 'Virtual Try-On API',    info: {    openapi: '3.0.0',  definition: {const options = {  "version": "1.0.0",
  "description": "Backend API for AI Virtual Try-On SaaS",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "db:migrate": "prisma migrate dev",
    "db:generate": "prisma generate",
    "db:studio": "prisma studio"
  },
  "keywords": ["saas", "ai", "virtual-tryon"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "cloudinary": "^1.41.0",
    "stripe": "^14.7.0",
    "@prisma/client": "^5.7.1",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "express-validator": "^7.0.1",
    "axios": "^1.6.2",
    "cookie-parser": "^1.4.6",
    "swagger-ui-express": "^4.6.3",
    "swagger-jsdoc": "^6.2.8"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "prisma": "^5.7.1"
  }
}

